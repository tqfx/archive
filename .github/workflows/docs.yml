name: docs
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          lfs: true
          submodules: true
      - uses: actions/checkout@v2
        with:
          lfs: true
          submodules: true
          repository: ${{ github.repository_owner }}/tqfx.github.io
          path: _/${{ github.repository_owner }}
      - name: Run apt-get
        run: |
          set -x
          sudo apt-get -y update
          sudo apt-get -y install doxygen graphviz mscgen dia
          sudo apt-get -y install lua5.4 liblua5.4-dev
      - uses: actions/checkout@v2
        with:
          repository: luarocks/luarocks
          path: luarocks
      - name: Install luarocks
        run: |
          set -x
          ./configure
          make
          sudo make install
        working-directory: luarocks
      - name: Run luarocks
        run: |
          set -x
          sudo luarocks install ldoc
      - name: Run makedoc
        run: |
          set -x
          cmake -S . -B build -DLIBA_DOXYGEN=1 \
                              -DLIBA_LUA=1 -DLIBA_LDOC=1 \
                              -DLIBA_JAVA=1 -DLIBA_JAVADOC=1
          cmake --build build --target adoc
          rm -rf _/${{ github.repository }}
          mv build/html _/${{ github.repository }}
          mv build/ffi/lua/doc _/${{ github.repository }}/lua
          mv build/ffi/java/javadoc/liba _/${{ github.repository }}/java
      - name: Run rustdoc
        run: |
          set -x
          rm -rf _/${{ github.repository }}/rust
          cargo doc --release --no-deps
          cp -r target/doc _/${{ github.repository }}/rust
      - uses: peaceiris/actions-gh-pages@v3
        with:
          deploy_key: ${{ secrets.ACTIONS_DEPLOY_KEY }}
          commit_message: ${{ github.event.head_commit.message }}
          external_repository: ${{ github.repository_owner }}/tqfx.github.io
          publish_dir: _/${{ github.repository_owner }}
      - name: Run autotools
        run: script/autogen
      - uses: actions/upload-artifact@v2
        with:
          if-no-files-found: ignore
          name: ${{ github.sha }}
          path: build/*.tar.*
